export interface UserRoles {
  id: string;
  name: string;
  descripcion: string;
  seleccionado: boolean

  // constructor(role: UserRoles) {
  //   this.id = role.id;
  //   this.name = role.name;
  //   this.descripcion = role.descripcion;
  //   this.seleccionado = role.seleccionado;
  // }
}
